/**
 * Supported Raw Value Types
 * @typedef {('string'|'number'|'boolean'|false)} RawTypes
 */

/**
 * Configuration Model
 * @typedef {Object} Config
 * @prop {RawTypes} [type]
 * @prop {boolean} [freeze]
 * @prop {boolean} [ignoreCase]
 */

/**
 * @typedef caseTypes
 * @type {(string|string[]|Object.<string, any>)}
 */
